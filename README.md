## Prerequisites
- Docker + Docker Compose installed
- (Optional) DataGrip for GUI access

## Run it
```bash
docker compose up --build
```

## Connect via DataGrip
- Host: `localhost`
- Port: `5432`
- Database: `testdb`
- User: `postgres`
- Password: `postgres`
